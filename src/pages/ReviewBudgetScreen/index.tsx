import React from 'react';
import { Container } from './styles';

import { ThemeText } from '../../components';

const ReviewBudgetScreen: React.FC = () => {
	return (
		<Container>
			<ThemeText>Meus pedidos</ThemeText>
		</Container>
	);
};

export default ReviewBudgetScreen;
