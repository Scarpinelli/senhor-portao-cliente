import React from 'react';
import { Container } from './styles';

import { ThemeText } from '../../components';

const RequestBudgetScreen: React.FC = () => {
	return (
		<Container>
			<ThemeText>Realizar orçamento</ThemeText>
		</Container>
	);
};

export default RequestBudgetScreen;
